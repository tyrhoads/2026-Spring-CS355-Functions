use std::num::ParseIntError;


/// Use the `parse` method to turn a string slice into an unsigned 32 bit integer.
/// If parsing fails... return the resulting ParseIntError
pub fn parse_an_int(raw_value: &str) -> Result<u32, ParseIntError> {
    todo!()
        //
}


/// Attempt to divide by zero. If rhs is 0, return None, otherwise return some quotient.
pub fn divide(lhs: i32, rhs: i32) -> Option<i32> {
    todo!()
}
